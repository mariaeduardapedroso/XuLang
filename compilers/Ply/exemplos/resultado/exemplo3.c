/* Código gerado automaticamente por xu_compiler.py */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    /* Declarações */
    int contador;

    /* Programa */
    contador = 1;
    while ((contador <= 5)) {
    printf("%d\n", contador);
    contador = contador + 1;
    printf("maria" "\n");
}

    return 0;
}